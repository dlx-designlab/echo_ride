import ip from '../ip';

// export const ip = 'localhost';
export const port = '3001';
export const url = `https://${ip}/`; // <<< use this syntax when running on github codespaces
// export const url = `https://${ip}:${port}/`; // <<< use this syntax when running locally

export const busNumber = 5;

